# --- Global arcade leaderboard (Supabase) ---
# Optional. Without these, scores stay per-device (localStorage).
# Put the REAL values in .env.local (which is gitignored), not here.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-PUBLIC-KEY
