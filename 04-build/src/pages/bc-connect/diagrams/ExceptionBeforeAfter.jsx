/**
 * DEPRECATED — safe to delete.
 *
 * A decorative before/after button diagram. Replaced when the Restraint
 * section was rewritten to tell the btn-creative story in prose. Nothing
 * imports this file. It is kept only because this sandbox cannot delete
 * files — remove it manually.
 */
export default function ExceptionBeforeAfter() {
  return null
}
