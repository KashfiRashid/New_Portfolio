/**
 * DEPRECATED — safe to delete.
 *
 * The Design Challenge "how might we" line was folded into Problem.jsx as
 * a closing beat during the targeted-consolidation pass. Nothing imports
 * this file. It is kept only because this sandbox cannot delete files —
 * remove it manually.
 */
export default function DesignChallenge() {
  return null
}
