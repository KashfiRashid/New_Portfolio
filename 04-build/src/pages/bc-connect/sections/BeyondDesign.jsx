/**
 * DEPRECATED — safe to delete.
 *
 * The Beyond Design content was folded into Solution.jsx as a closing
 * running-text paragraph during the targeted-consolidation pass. Nothing
 * imports this file. It is kept only because this sandbox cannot delete
 * files — remove it manually.
 */
export default function BeyondDesign() {
  return null
}
